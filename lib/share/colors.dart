import 'package:flutter/material.dart';

class AppColor {
  /// Primary color - used for AppBar, primary buttons, and main branding elements
  static const Color primary = Color(0xff1B262C);

  /// Secondary color - used for secondary buttons, tabs, and highlight areas
  static const Color secondary = Color(0xff0F4C75);

  /// Accent color - used for icons, badges, tags, and small attention-grabbing UI
  static const Color third = Color(0xff3282B8);

  /// Background color - used for screen backgrounds, cards, and input fields
  static const Color bg = Color(0xffBBE1FA);
}
